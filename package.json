{
  "name": "fire-safety-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Fire Safety Products & Services platform — client (customer PWA), admin (staff dashboard), server (Express + MongoDB API)",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT,ADMIN -c blue,green,magenta \"npm:server\" \"npm:client\" \"npm:admin\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "admin": "npm run dev --prefix admin",
    "build": "npm run build --prefix server && npm run build --prefix client && npm run build --prefix admin",
    "seed": "npm run seed --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client && npm run lint --prefix admin",
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
