# This is a reference only — each app has its own .env.example (server/, client/, admin/).
# Copy each one to .env in its respective folder and fill in real values.
# See server/.env.example for the full backend configuration (MongoDB, JWT, Cloudinary, email, payments, SMS/WhatsApp, cron).
# See client/.env.example and admin/.env.example for frontend API URLs.
